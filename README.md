# 🧭 The Fontana Engine Core (v4.0)
### An AuDHD-Optimized Cross-Language AI Text Synthesis & Screenplay Architecture

Built by **Alistair Fontaine** in Bulawayo, Zimbabwe.

The Fontana Engine Core is a high-performance, hybrid-language neural text synthesis platform designed to eliminate corporate abstraction and enforce an absolute, step-by-step **Order of Operations (O³)**. Built with a strict focus on memory safety, defensive coding protocols, and persistent background multi-pass process isolation, this framework contains no massive cloud wrappers or black-box APIs—every weight matrix forward pass, subword syllable matching lookup, and stateful lookback context array stream is manually written and compiled natively.

---

## 🛠️ The Ultimate Rescue Compilation Chain
If the runtime ever scrambles, cross-origin network queries deadlock, or unexpected subprocess errors try to freeze your workspace, execute this exact unbroken production pipeline string directly inside your Linux shell terminal to force a clean cache swap, re-allocate the socket lines, and deploy the safe engine baseline:

```bash
pkill -f tensor_engine_binary && pkill -f uvicorn && fuser -k 8000/tcp && fuser -k 8080/tcp && g++ -std=c++17 backend/tensor_engine.cpp -o backend/tensor_engine_binary && export PYTHONPATH=PYTHONPATH:(pwd)/core && python3 -m uvicorn core.app:app --host 127.0.0.1 --port 8000 --reload
```

---

## 🚀 Completed Milestones & Architectural Core

### 1. Hybrid Cross-Language Ingestion Architecture
- **Persistent Subprocess Background Daemon:** Boots your ultra-fast compiled C++ engine core once into volatile RAM cache storage tracks via long-lived asynchronous `subprocess.Popen` pipelines, keeping the neural parameters hot and responsive for sub-millisecond thread execution.
- **Asynchronous REST API Gateway:** Full FastAPI network abstraction layer utilizing non-blocking asynchronous event loops to pipe JSON telemetry tokens cleanly back and forth over local loopback ports.

### 2. High-Density Subword Syllable Tokenization
- **108-Line Regex BPE Pattern Matcher:** Independent implementation of a syllable-aware subword extraction utility. Uses optimized sorting regular expressions to look for custom structural screenplay blocks (like `fontana`, `istair`, `brain`, `logic`, `code`, `system`, `tech`, `auto`) and fuse them natively on startup to prevent character-splitting stutter noise loops.
- **Dynamic Vocabulary Mapping Layout:** Dynamic disk dictionary tracking utilizing persistent metadata descriptors (`vocab_meta.json`) to track exact reverse-vocabulary indices smoothly.

### 3. Stateful Long-Range Sequence Calibration
- **Softmax Matrix Probability Scaling:** Fine-tuned activation layers inside `backend/tensor_engine.cpp` with a fluid long-range temperature floor scaling between `0.18f` and `0.32f` past 4 tokens to provide natural creative entropy breathing space and crush transitional character static.
- **Broadened Selection Pool Truncation:** Expands token lookahead pathways via customized `Top-K` (`K = 6`) selection filter gates, normalizing the discrete probability distribution pools directly inside standard template vector maps.

### 4. Stateful Session Memory & Brutalist Command Workspace
- **Rolling Context Lookback History Buffer:** Stateful array caches running inside `core/app.py` that intercept your prompt streams, bundle the last 3 generated sentences dynamically, and feed them back down the pipe to enable deep, continuous, multi-page screenplay scene construction.
- **Brutalist Antagonist vs. Opium Web UI Vault:** High-contrast minimalist frontend interface built from scratch in pure HTML/CSS. Features a non-blocking class-based hot-swapper to flip between **Antagonist Green (`#00ff66`)** and **Opium Crimson Red (`#ff0055`)** directly inside Firefox with a single mouse click.
- **One-Click Markdown Scenario Exporter:** Integrated JavaScript file pipeline handler that captures your live generation logs, packages them into structured screenplay markdown files (`.md`), and automatically triggers local browser downloads on impact lock.

---

## 📂 Project Pipeline Structure

- `/backend` — Highly compiled C++ tensor array processing, weight loading, and softmax inference logic loops.
- `/core` — Python REST infrastructure wrappers, stateful memory buffers, and 108-line regular expression character tokenizers.
- `/scripts` — Automated Linux Bash data-stride optimization controllers and screenplay asset extraction scripts.
- `/weights_backup` — Parameter stashing repository lanes to secure verified memory profiles on your partition.

---

## 🗺️ Chronological Order of Operations (OOO) Roadmap

- [x] Phase A: Standard Subprocess Pipeline Communication Sockets & I/O Mapping
- [x] Phase B: 108-Line Custom Regular Expression Subword Syllable Matching Engine
- [x] Phase C: 512-Dimensional Hidden Layer Embedding Map Matrices Configuration
- [x] Phase D: Non-Blocking Asynchronous REST Web Gateway API Server Implementation
- [x] Phase E: Premium Dark-Minimalist Antagonist Dashboard Grid Interface Layout
- [x] Phase F: One-Click Scenario Logging & Markdown Script Exporter Module
- [x] Phase G: Stateful Session Memory History Buffer Rolling Caches
- [x] Phase H: On-Click Brutalist Antagonist vs. Opium Crimson Red Theme Toggler Toggle
- [ ] **Phase I: Bulk Ingest Large Public-Domain Gothic Literature Master Books (Next Operation)**
- [ ] Phase J: Implement Multi-User Isolated Session Memory Space Allocation Channels
- [ ] Phase K: Connect Live Graph Parameter Visualizers to Track Tensor Weights in Real-Time

---

## 🔧 Build Requirements & Infrastructure

### Core Stack
- **Languages:** Modern C++17 & Python 3.12+
- **Network Application Server:** FastAPI & Uvicorn (StatReload Profile Enabled)
- **Local Interface Gateway:** Firefox Web Browser Module
- **Operating Environment:** Linux Storage Partition Environment (Debian/Ubuntu Core)

### System Package Setup (Debian/Ubuntu Linux Partition)
```bash
sudo apt-get update
sudo apt-get install build-essential python3-pip python3-fastapi uvicorn curl
```
